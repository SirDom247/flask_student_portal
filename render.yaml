services:
  - type: web
    name: pde-portal
    env: docker
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true